using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class DEbUGTEXT : Cirrus.BaseSingleton<DEbUGTEXT>
{
    public UnityEngine.UI.Text text => GetComponent<UnityEngine.UI.Text>();

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
